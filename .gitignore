# secrets
.env
.env.*
!.env.example

# python
cv-service/.venv/
__pycache__/
*.pyc
.pytest_cache/

# node
node_modules/
frontend/dist/

# generated images (the run records in experiments/runs and the site snapshot are versioned on purpose)
dataset/processed/

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
docs/report/*.html

# the judgements of the blind study are written on the server by cv-service; copy them
# back with scp and `git add -f experiments/judgments.csv` when the study is over
experiments/judgments.csv
experiments/choices.csv
